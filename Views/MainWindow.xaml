<Window x:Class="PhotoAlbum.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PhotoAlbum"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Path=Locations}" SelectedItem="{Binding Path=CurrentLocation}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <Label FontSize="18" Content="{Binding Path=Name}"/>
                        <Label FontSize="18" Content="{Binding Path=Date, StringFormat=dd.MM.yyyy}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <b:Interaction.Triggers>
                <b:EventTrigger EventName="SelectionChanged">
                    <b:InvokeCommandAction Command="{Binding Path=CommandChangeLocation}"/>
                </b:EventTrigger>
            </b:Interaction.Triggers>

        </ListBox>

        <Border Grid.Column="1" Height="375" Width="500" BorderThickness="1" BorderBrush="DarkGray">
            <Image Source="{Binding Path=CurrentPhoto.Path}"/>
        </Border>
    </Grid>
</Window>
